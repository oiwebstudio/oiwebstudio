# Dependencies
node_modules/
npm-debug.log
yarn-error.log

# Build & deployment
dist/
build/
.env
.env.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local development
.claude/

# Todo el material que no forma parte de la web desplegada vive en _local/.
# Si algo no debe publicarse, se mueve ahí y no hace falta tocar este fichero.
_local/

# Documentación de trabajo. docs/PUBLICAR.md sí se versiona (explica el
# despliegue); el resto son notas, prompts y borradores que se quedan en local.
docs/LEEME.md
docs/INFORME-AUDITORIA.md
docs/PROMPT-*.md
docs/cm-semana-1.txt
docs/estrategia-de-ventas.txt

*.bat
localai/

# Biblioteca de animaciones: sólo se publica web/biblioteca.html.
# La carpeta de trabajo — fuente, catálogo, brief e investigación — se
# queda en local: es el criterio, y eso no se regala.
biblioteca/animaciones/

# Versión en euskera: se genera en cada despliegue con
# scripts/generar-euskera.mjs desde las páginas en castellano.
# No editar a mano ni commitear.
web/eu/
