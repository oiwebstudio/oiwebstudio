<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/negro</item>
        <item name="colorPrimaryDark">@color/negro</item>
        <item name="colorAccent">@color/azul_ios</item>
    </style>

    <!-- Sin barra de acción y con fondo negro: la interfaz la pinta la web. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@color/negro</item>
        <item name="android:statusBarColor">@color/negro</item>
        <item name="android:navigationBarColor">@color/negro</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@color/negro</item>
        <item name="android:statusBarColor">@color/negro</item>
    </style>
</resources>
